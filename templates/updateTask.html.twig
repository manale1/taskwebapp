{% extends 'base.html.twig' %}
 {% block titleTwig %}Update Task{% endblock %}
{% block body %}
    <div class="contentTwig container" style="width: 800px">
        {{ form_start(formtache) }}
        {{ form_widget(formtache) }}
        <h4>Tool</h4>
        {{ (formOutil)|raw }}
        <button type="submit" style="margin-top: 15px" class="button" >Register</button>
        {{ form_end(formtache) }}
    </div>
{% endblock %}

{% block title %}

{% endblock %}

