{% extends 'base.html.twig' %}

{% block body %}
    <div class="contentTwig">
        {{ form_start(formtache) }}
        {{ form_widget(formtache) }}
        <div>
            {{ formOutil|raw}}
        </div>
        <button type="submit" style="margin-top: 15px" class="btn btn-primary btn-lg">Register</button>
        {{ form_end(formtache) }}
    </div>

{% endblock %}

{% block title %}

{% endblock %}

