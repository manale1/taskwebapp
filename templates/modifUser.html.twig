{% extends 'base.html.twig' %}
{% block titleTwig %}Update user{% endblock %}
{% block body %}
    <div class="container" style="width: 800px;">
        {{ form_start(formEmploye) }}
        {{ form_widget(formEmploye) }}
        <button type="submit" style="margin-top: 15px" class="button" >Register</button>
        <button type="reset" style="margin-top: 15px" class="button" ><a href="{{ path('app_m') }}">Cancel</a></button>
        {{ form_end(formEmploye) }}
    </div>


{% endblock %}

{% block title %}
Uppdate user
{% endblock %}

